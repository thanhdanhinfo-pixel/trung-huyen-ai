name: SYSTEM_AUTONOMY_CORE_TOOL_REGISTRY
status: active

mcp_tools:
  pingMcp:
    status: healthy
  listDirectory:
    status: healthy
  readFile:
    status: partially_healthy
    note: readFile works for existing files, but returns Internal Server Error for missing paths instead of clean 404.
  callMcpTool:
    status: healthy
  readDrivePath:
    status: available

github_operations:
  read_repository: healthy
  write_repository: healthy
  commit: healthy

missing_tools:
  cloud_run_logs: permission_pack_created_but_not_enabled
  cloud_run_revision_list: permission_pack_created_but_not_enabled
  cloud_run_rollback: permission_pack_created_but_not_enabled_requires_approval
  cloud_build_status_reader: permission_pack_created_but_not_enabled
  github_check_status_reader: permission_pack_created_but_not_enabled
  local_shell: unavailable_restricted_design_required
  local_python_import_test: permission_pack_created_but_not_enabled
  import_graph_scanner: permission_pack_created_but_not_enabled
