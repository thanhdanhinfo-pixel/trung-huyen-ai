# HANDOVER - 2026-06-26

## Hoàn thành

- MCP runtime hoạt động.
- /mcp/ping OK.
- /mcp/list-directory OK.
- /mcp/read-file OK.
- /mcp/search-code OK.
- /mcp/read-drive-path OK.
- /runtime/errors OK.
- /deployment/smoke-test OK.
- Runtime error registry đã hoạt động.
- Deployment router đã được thêm.
- GitHub runtime capability registry đã tồn tại.
- openapi-mcp-v3.json đã được cập nhật lên version 3.3.0.
- Đã thêm /mcp/call vào OpenAPI để GPT có thể gọi backend qua callMcpTool.

## Đã xác nhận

- Có thể đọc GitHub qua MCP direct endpoints.
- Có thể đọc Google Drive theo logical path.
- Có thể cập nhật GitHub bằng /github/update.
- Có thể patch GitHub bằng /github/patch.
- Có thể chạy smoke test sau deploy.

## Chưa hoàn thành

### Automatic rollback

Hiện tại:

- /deployment/rollback vẫn là stub.
- Chưa revert commit tự động.
- Chưa redeploy tự động.

### GPT Action Runtime

Đã thêm:

- /mcp/call
- callMcpTool

Cần làm tiếp:

- Re-import openapi-mcp-v3.json trong GPT Builder.
- Nhấn Update GPT.
- Mở conversation mới.
- Test callMcpTool với /health.
- Xác nhận ClientResponseError không còn xảy ra trong phiên mới.

## Nhiệm vụ tiếp theo

1. Xác nhận callMcpTool hoạt động.
2. Xây cleanup planner:
   - scan repository
   - phát hiện file không dùng
   - preview danh sách
   - yêu cầu approval
   - chỉ delete khi được duyệt

3. Thiết kế rollback an toàn:
   - rollback-preview trước
   - approved=true mới rollback thật
   - smoke-test sau rollback

## Quy tắc vận hành

- Mặc định commit=false.
- Delete phải có approval rõ ràng.
- Cleanup phải preview trước.
- Smoke test sau mọi thay đổi runtime.
- Nhà sáng lập giữ quyền quyết định cuối cùng.
