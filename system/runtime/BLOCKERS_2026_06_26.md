# Runtime Blockers — 2026-06-26

## BLOCKER-001 

Name: Runtime still running old Cloud Run image.

Evidence:

- POST /deployment/redeploy -> 404
- GET /deployment/capabilities -> old schema
- GET /mcp/tools -> missing drive_list_children, drive_tree, drive_index

Resolution:

Perform one external deployment using one of:

1. scripts/activate_deploy_workflow.py
2. PROJECT_ID=<gcp-project> ./deploy_cloud_run.sh
3. Cloud Build trigger via cloudbuild.yaml

## BLOCKER-002

Name: GitHub token lacks workflow permission.

Evidence:

- 403 Forbidden when writing .github/workflows/deploy-cloud-run.yml

Resolution:

Use PAT with scopes:

- repo
- workflow

## PHASE FREEZE

Do not execute:

- Repository cleanup
- Rollback automation
- Autonomous coordination rollout
- Large-scale refactors

until runtime activation smoke tests pass.
